#pragma once
#include <iostream>
#include "ehstructs.h"
using namespace std;


static int parser(void *datam, int argc, char **argv, char **azColName) {
	int i;
	for (i = 0; i < argc; i++)
	{
		counter = counter++;
	}

	for (i = 0; i<argc; i++) {
		store[j].names = argv[i];

		store[j].gid = stoi(store[j].names);
		j++;
	}
	return 0;
}

static int extractgid(void *datam, int argc, char **argv, char **azColName) {
	int i;
	for (i = 0; i < argc; i++)
	{
		incre = incre++;
	}

	for (i = 0; i<argc; i++) {
		receipt[k].temp = argv[i];
		receipt[k].galleryid = stoi(receipt[k].temp);

		k++;

	}
	return 0;
}

static int extracttk(void *datam, int argc, char **argv, char **azColName) {
	int i;
	for (i = 0; i < argc; i++)
	{
		addi = addi++;
	}

	for (i = 0; i<argc; i++) {
		receipt[l].token = argv[i];
		l++;

	}
	return 0;
}
static int mangaidex(void *datam, int argc, char **argv, char **azColName) {
	int i;
	for (i = 0; i < argc; i++)
	{
		count1 = count1++;
	}

	for (i = 0; i<argc; i++) {
		store[h].temp = argv[i];

		store[h].mangaID = stoi(store[h].temp);
		h++;
	}
	return 0;
}
static int call(void *datam, int argc, char **argv, char **azColName) {
	
	return 0;
}